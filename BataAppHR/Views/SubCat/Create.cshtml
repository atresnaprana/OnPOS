@model OnPOS.Models.dbSubCategory
@{
    ViewData["Title"] = "Create New Subcategory";
}

<div class="auth-container">
    <form asp-action="Create" id="formcreate" method="post">
        <div class="text-center">
            <h1>@ViewData["Title"]</h1>
            <p class="text-muted">Complete the details below to add a new item subcategory.</p>
        </div>

        <hr class="my-4" />

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

        <div class="form-group">
            <label asp-for="Category"></label>
            <!-- ** LOGIC PRESERVED: Your DropDownListFor helper and select2 ID are untouched ** -->
            @Html.DropDownListFor(m => m.Category, new SelectList(Model.ddcat, "Category", "description"), "-- Select a Parent Category --", new { @class = "form-control" })
            <span asp-validation-for="Category" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="SubCategory">Subcategory Name</label>
            <input asp-for="SubCategory" class="form-control" placeholder="e.g., MEN'S FORMAL" />
            <span asp-validation-for="SubCategory" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="description">Description</label>
            <input asp-for="description" class="form-control" placeholder="A brief description of the subcategory" />
            <span asp-validation-for="description" class="text-danger"></span>
        </div>

        <div class="d-flex flex-column gap-2 mt-4">
            <!-- ** LOGIC PRESERVED: This button calls your alertfirst() function ** -->
            <input type="button" value="Save" onclick="alertfirst()" class="btn btn-primary" />
            <a asp-action="Index" class="btn btn-secondary text-center">Kembali ke List</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <!-- ** UNTOUCHED LOGIC: Your original script is preserved ** -->
    <script>
        $(document).ready(function () {
            // ** LOGIC PRESERVED: Your select2 initialization is here **
            $("#Category").select2({
                placeholder: "Select Category"
            });
            // This is a slightly more robust way to clear the initial value
            $("#Category").val(null).trigger('change');
        });

        function allowOnlyNumber(evt) {
            var charCode = (evt.which) ? evt.which : event.keyCode
            if (charCode > 31 && (charCode < 48 || charCode > 57))
                return false;
            return true;
        }

        function alertfirst() {
            if (confirm("Are you sure you want to save this data?")) {
                $('#formcreate').submit();
            } else {
                return false;
            }
        }
    </script>
}