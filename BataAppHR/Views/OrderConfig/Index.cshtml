@model IEnumerable<BataAppHR.Models.dbOrderConfig>
@{
    ViewData["Title"] = "Form OrderConfig";

    var isAdmin = User.IsInRole("Admin").ToString();
}

<style>
    #myPopup {
        display: none;
        position: absolute;
        top: 50px;
        right: 50%;
        background-color: blue;
        height: 100px;
        width: 100px;
    }

    #myExit {
        position: absolute;
        right: 0px;
        text-align: right;
        color: white;
        background-color: red;
    }

    .btn-primary2 {
        color: #ffffff;
        background-color: #000000;
        border-color: #000000 !important
    }

        .btn-primary2:hover {
            color: #fff;
            background-color: #be0000;
            border-color: #be0000 !important
        }
</style>
<p>
</p>
<div class="container" id="bootstrap-overrides">
    <div class="row">
        <div class="col-sm align-self-end">
            <a asp-action="Create" class="btn btn-dark">Input Data Baru</a>
        </div>

    </div>
</div>
<br />
<table class="table table-striped" id="IndexTbl">
    <thead>
        <tr>
            <th>Num.</th>
            <th>
                Pairs Article
            </th>
            <th>
                Pairs Order
            </th>
            <th>
                Update Date
            </th>
            <th>
                Update User
            </th>
            <th>
                Entry Date
            </th>
            <th>
                Entry User
            </th>
           
        </tr>
    </thead>
    <tbody id="tbodyid">
        @{
            var i = 1;
        }
        @foreach (var dt in Model)
        {
        <tr>
            <td>
                @i
            </td>
            <td>
                @Html.DisplayFor(modelItem => dt.pairsarticle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => dt.pairsorder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => dt.UPDATE_DATE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => dt.UPDATE_USER)

            </td>
            <td>
                @Html.DisplayFor(modelItem => dt.ENTRY_DATE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => dt.ENTRY_USER)

            </td>
            @*<td>
                <div>
                    <a asp-action="Edit" asp-route-id="@dt.idTrainer" class="btn btn-sm btn-dark">Edit</a>
                    &nbsp;
                    @if (User.IsInRole("Admin"))
                    {
                        <a asp-action="Delete" asp-route-id="@dt.idTrainer" class="btn btn-sm btn-dark">Delete</a>

                    }
                </div>
            </td>*@
        </tr>
            i++;
        }
    </tbody>
</table>

@section Scripts{
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script>
    $(document).ready(function () {
        $('#IndexTbl').DataTable();
    });
    </script>
}

