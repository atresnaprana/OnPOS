@model OnPOS.Models.dbStoreList

@{
    ViewData["Title"] = "Confirm Deactivation";
}

<div class="auth-container">
    <div class="danger-card-header">
        <div class="danger-icon"><i class="fa-solid fa-triangle-exclamation"></i></div>
        <h1>@ViewData["Title"]</h1>
        <p class="text-muted">Are you sure you want to deactivate this store? It will be marked as inactive.</p>
    </div>

    <div class="item-details">
        <p><strong>Store Name:</strong> <span class="item-value">@Html.DisplayFor(model => model.STORE_NAME)</span></p>
        <p><strong>Manager:</strong> <span class="item-value">@Html.DisplayFor(model => model.STORE_MANAGER_NAME)</span></p>
        <p><strong>Phone:</strong> <span class="item-value">@Html.DisplayFor(model => model.STORE_MANAGER_PHONE)</span></p>
    </div>

    <form asp-action="Delete" method="post">
        <!-- ** LOGIC PRESERVED: Hidden input for the ID is still here ** -->
        <input type="hidden" asp-for="id" />

        <div class="d-grid gap-2">
            <input type="submit" value="Yes, Deactivate this Store" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">No, Cancel</a>
        </div>
    </form>
</div>