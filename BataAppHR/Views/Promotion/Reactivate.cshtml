@model OnPOS.Models.dbDiscount

@{
    ViewData["Title"] = "Reactivate";
}

<h1>Reactivate</h1>

<div>
    <h4>Discount Info</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            <label>Discount name: </label> @Html.DisplayFor(model => model.promo_name)
        </dt>
        <dt>
            <label>article: </label>@Html.DisplayFor(model => model.article)
        </dt>

    </dl>
    <form asp-action="Reactivate">
        <input type="hidden" asp-for="id" />
        <input type="submit" value="Reactivate" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-primary">Back to list</a>
    </form>
</div>